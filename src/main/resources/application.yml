spring:
  application:
    name: products-api

  datasource:
    url: jdbc:h2:mem:productsdb
    driver-class-name: org.h2.Driver
    username: sa
    password:

  sql:
    init:
      mode: always

  cache:
    type: simple

logging:
  level:
    root: INFO
    com.mango.products: DEBUG

server:
  port: 8080

